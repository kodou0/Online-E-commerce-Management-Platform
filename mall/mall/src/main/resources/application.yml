spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/eshop?allowMultiQueries=true&useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: 111111
#修改内嵌服务器端口号
server:
  port: 8098

mybatis-plus:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.lc.model.entity
  global-config:
    db-config:
      id-type: auto

logging:
  level:
    com.lc.mapper: debug

pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true